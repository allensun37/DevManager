# Copy this file to deploy/docker/.env and replace the placeholder locally.
# Never commit a real API key.
DEVMANAGER_API_KEY=replace-with-a-local-development-key

# Optional loopback host port exposed by Docker Compose.
DEVMANAGER_PORT=8080
